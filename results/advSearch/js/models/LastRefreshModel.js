define([
	"backbone"
], function(Backbone){
	return Backbone.Model.extend({
		// initialize: function(options) {
		// 	this.isCompleted = options.isCompleted;
		// },
	 //    urlRoot: '/explorer/api/advsearch/lastrefresh',
		// url: function() { 
		// 	var params = '';
		// 	if(this.isCompleted){
		// 		params = "?is_completed="+this.isCompleted;
		// 	}
		// 	return this.urlRoot+params; 
		// }
	});	
});